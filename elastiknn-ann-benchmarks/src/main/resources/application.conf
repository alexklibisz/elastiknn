elastiknn {
    annb {
        storage {
            datasets = "/tmp"
            index = "/tmp"
            results = "/tmp"
        }
        runtime {
            parallelism = "auto"
        }
    }
}

akka {
    loggers = ["akka.event.slf4j.Slf4jLogger"]
}